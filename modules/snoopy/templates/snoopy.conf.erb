destination d_snoopy { 
	file("<%= scope.lookupvar('snoopy::params::snoopy_logfile') %>"); 
};

filter f_snoopy { 
	program("snoopy"); 
};

log { 
	source(s_sys); 
	filter(f_snoopy); 
	destination(d_snoopy); 
	flags(final); 
};
