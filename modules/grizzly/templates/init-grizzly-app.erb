#!/bin/bash
#
# ******************
# Managed by Puppet
# ******************
#
# tomcat      This shell script takes care of starting and stopping spark
#
# chkconfig: - 80 20
#
### BEGIN INIT INFO
# Provides: spark
# Required-Start: $network $syslog
# Required-Stop: $network $syslog
# Default-Start:
# Default-Stop:
# Short-Description: start and stop tomcat
### END INIT INFO

start() {
  <%= @start_command %>
}

stop() {
  <%= @stop_command %>
}

status() {
  <%= @status_command %>
}
case $1 in
  start)
      start
      ;;
  stop)
      stop
      ;;
  restart)
      stop
      start
      ;;
  status)
      status
      ;;
  *)
      echo "Usage: $0 {start|stop|restart|status}"
      echo
      exit 1
      ;;
esac
